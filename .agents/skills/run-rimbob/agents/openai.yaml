interface:
  display_name: "Run RimBob"
  short_description: "Build, start, and verify the RimBob.Host server"
  default_prompt: "Use $run-rimbob to build and start the RimBob.Host server, then verify the dashboard URL, runtime root, process path, and startup checks."
