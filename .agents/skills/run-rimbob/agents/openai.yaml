interface:
  display_name: "Run RimBob"
  short_description: "Start and verify the RimBob.Host server"
  default_prompt: "Use $run-rimbob to start the already-built RimBob.Host server, then verify the dashboard URL, runtime root, process path, and startup checks. Use positive build/setup flags only when needed."
